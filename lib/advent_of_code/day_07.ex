defmodule AdventOfCode.Day07 do
  def parse_rules(rules) do
  end

  def part1(args) do
    rules = parse_rules(args)
    rules
    |> String.trim
    |> String.split("\n")
    |> String.split("\n")
  end

  def part2(_args) do
  end
end
