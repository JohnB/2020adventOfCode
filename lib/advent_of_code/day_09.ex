defmodule AdventOfCode.Day09 do
  # Implorting AdventOfCode provides these helpers:
  # as_single_lines(args)
  # as_doublespaced_paragraphs(args, fn(paragraph) -> end)
  # as_paragraph_lines(paragraph)
  # delimited_by_spaces(text)
  # delimited_by_colons(text)
  import AdventOfCode

  def part1(args) do
#    as_single_lines(args)
#    |> Enum.reduce(fn(line) ->
#      [[left | right]] = delimited_by_colons(line)
#    end)
  end

  def part2(_args) do
  end
end
